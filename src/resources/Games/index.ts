import { Gamepad as GameIcon } from '@material-ui/icons';

import GameList from './GameList';
import GameCreate from './GameCreate';
import GameEdit from './GameEdit';

export default {
  list: GameList,
  create: GameCreate,
  edit: GameEdit,
  icon: GameIcon,
};
